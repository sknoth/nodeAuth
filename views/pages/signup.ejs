<!DOCTYPE html>
<html lang="en">
<% include ../partials/head %>
<body class="container">

    <div class="page-header text-center">
      <h1><span class="fa fa-sign-in"></span> Signup</h1>
    </div>
  <% include ../partials/header %>

  <!-- show any messages that come back with authentication -->
  <% if (message.length > 0) { %>
      <div class="alert alert-danger"><%= message %></div>
  <% } %>

  <main class="row">
    <div class="col-sm-4 col-sm-offset-4">
      <!-- LOGIN FORM -->
      <form action="/signup" method="post">
        <div class="form-group">
            <label>Email</label>
            <input type="text" class="form-control" name="email">
        </div>
        <div class="form-group">
            <label>Password</label>
            <input type="password" class="form-control" name="password">
        </div>

        <div class="form-group">
          <label>Role</label>
          <div class="dropdown">
            <input class="span2" id="userrole" name="userrole" value="1" type="hidden">
            <button class="btn btn-default dropdown-toggle"
              type="button" id="userrole-select" name="userrole-select" data-toggle="dropdown"
              aria-haspopup="true" aria-expanded="true">
              <span class="role">Patient</span>
              <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" aria-labelledby="userrole-select">
              <li onclick="$('#userrole').val(1); $('#userrole-select>.role').html('Patient')">
                <a href="#">Patient</a></li>
              <li onclick="$('#userrole').val(2); $('#userrole-select>.role').html('Physician')">
                <a href="#">Physician</a></li>
              <li onclick="$('#userrole').val(3); $('#userrole-select>.role').html('Researcher')">
                <a href="#">Researcher</a></li>
            </ul>
          </div>
          <p class="help-block">For convenient testing please select a role for your new user here</p>
        </div>
        <button type="submit" class="btn btn-warning btn-lg">Signup</button>
      </form>

      <hr>

      <p>Already have an account? <a href="/login">Login</a></p>
      <p>Or go <a href="/">home</a>.</p>
    </div>
  </main>

  <% include ../partials/footer %>

</body>
</html>
